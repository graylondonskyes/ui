# ui

A dependency-free static frontend for the ui interface studio site.

## Scripts

- `npm run build` builds the site into `dist/`.
- `npm run smoke` checks key files, anchor targets, wired CTAs, theme persistence, visual canvas, and responsive CSS.
- `npm test` runs the smoke checks.
